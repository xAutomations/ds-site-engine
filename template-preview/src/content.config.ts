// The template harness intentionally has no content collections. Defining an empty
// config here prevents Astro from discovering the engine's client-backed collection
// config in the parent project.
export const collections = {};
